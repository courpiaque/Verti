<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Verti"
             x:Class="Verti.MainPage">

    <StackLayout BindingContext="{x:Reference slider}">
        <Label Text="{Binding Value, StringFormat='Words per minute: {0:0}'}"
               x:Name="wpm_text"
               HorizontalOptions="Center">
        </Label>
        <Slider Maximum="600" Minimum="60" x:Name="slider"/>
        <!-- Place new controls here -->
        <Button Text="Welcome, please select file to read!"
           x:Name="main_text"
           Clicked = "Select_File"
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />

        <Label Text="{Binding Word}"/>

        <Button
            x:Name="Start"
            Clicked="Start_Clicked"
            Text="Start"/>
        <Button
            x:Name="Library"
            Clicked="Library_Clicked"
            Text="Library"/>
    </StackLayout>

</ContentPage>
